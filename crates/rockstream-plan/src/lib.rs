//! PlanNode IR and physical OpNode graph for RockStream.
//!
//! Defines the `PlanNode` enum (the PlanIR) and the physical `OpNode` graph
//! used for execution.

#[cfg(test)]
mod tests {
    #[test]
    fn plan_crate_compiles() {}
}
