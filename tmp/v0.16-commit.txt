v0.16.0: workload DDL and view lifecycle

Implements the v0.16 roadmap milestone: CREATE WORKLOAD with
FRESHNESS_SLO, MEMORY_LIMIT, PRIORITY; workload assignment on
CREATE MATERIALIZED VIEW (WITH WORKLOAD = name); namespace-level
workload default (ALTER NAMESPACE ... SET DEFAULT WORKLOAD);
per-view PAUSE/RESUME MATERIALIZED VIEW; view dependency metadata;
SHOW VIEW STATUS FOR NAMESPACE; SHOW BACKFILL STATUS FOR
MATERIALIZED VIEW.

Changes:
- crates/rockstream-types: add workload module (WorkloadDef,
  FreshnessSlo, MemoryLimit, WorkloadPriority) and view_lifecycle
  module (ViewState, ViewStatus, BackfillStatus); add WorkloadId
  to ids.rs; add RS-1005 through RS-1008 error codes; add
  serde_json dev-dep
- crates/rockstream-catalog: add workload_store module
  (WorkloadStore); extend CatalogEntry with state/workload_name/
  depends_on/backfill_started_epoch; extend CatalogStore with
  workload DDL methods, pause_view/resume_view, show_view_status,
  show_backfill_status, register_view_with_options; add 11 v0.16
  proof tests; add 4 new CatalogError variants for RS-1005..1008
- Cargo.toml: bump workspace version to 0.16.0
- ROADMAP.md: mark v0.16 Done
- sign-offs/v0.16.md: full exit-criteria sign-off
