v0.18.0: SQL Alpha soak

- Add PlanNode::Join lowering to SqlFrontend: inner and cross joins lower
  to PlanNode::Join with AND-folded equijoin conditions or constant-true
  for cross joins (new lower_join_condition helper).

- Add rockstream sql subcommand to the binary: registers built-in demo
  schema (orders/products/events), plans SQL via DataFusion, lowers to
  PlanNode IR, and prints EXPLAIN INCREMENTAL output.

- SQL Alpha soak test module: join lowering, cross join, three-way union,
  DDL parse (CREATE VIEW / CREATE MATERIALIZED VIEW), explain integration,
  all-Phase-1-operator correctness soak.

- Deterministic SQL Alpha fuzzer (SEED=0x5EED_1850_ADEF_0018, 256 iters):
  generates random Phase 1 PlanNode trees and verifies no panic, every
  aggregate annotated, explain stable, re-runs identical (seed stability).

- v0.18 catalog proof tests: join round-trip through codec, Filter-Join-
  Aggregate round-trip through store, DiffCtx no-divergence across catalog
  load/in-memory path, all Phase 1 operators annotated consistently.

- Bump workspace version to 0.18.0.
- Mark v0.18 Done in ROADMAP.md.
- Add sign-offs/v0.18.md.
