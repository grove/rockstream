v0.15.0: DISTINCT and set operations on WeightAdd/v1

Implements SQL DISTINCT, UNION ALL, UNION, INTERSECT ALL, INTERSECT,
EXCEPT ALL, and EXCEPT as incremental DBSP operators backed by
WeightAdd/v1 weight accounting and zero-crossing state tracking.

Changes:
- crates/rockstream-ops: add `distinct` module (DistinctOp) and
  `set_ops` module (UnionAllOp, UnionOp, IntersectAllOp, IntersectOp,
  ExceptAllOp, ExceptOp); documents CLAMP_NOT_A_LAW not_merge_safe_reason
- crates/rockstream-oracle: add `set_op_oracle` module (SetOpOracle)
  with compute_distinct/union_all/union/intersect_all/intersect/
  except_all/except batch reference methods; add `set_op_tests`
  integration suite (15 tests, 9 proptest property tests)
- Cargo.toml: bump workspace version to 0.15.0
- ROADMAP.md: mark v0.15 Done
- sign-offs/v0.15.md: full exit-criteria sign-off
