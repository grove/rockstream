v0.23.0: Bootstrap and snapshot mode

- Add PlanNode::Snapshot + OpKind::Snapshot to plan IR
- DiffCtx: Snapshot -> Stateless (insert-only, no arrangement)
- SnapshotOp: streamed bootstrap epochs with batch_size cap
- Bootstrap frontier: is_complete() + BootstrapPhase enum
- Reconciliation: resume_from(N) skips committed rows on restart
- RS-1010 error code registered for connector position loss
- BootstrapOracle: batch reference for bootstrap correctness
- 11 proof tests: all-rows delivery, oracle match, resume no-gaps,
  resume no-duplicates, is_complete, plan codec, diff law,
  explain label, 100k-row scale, idempotent replay,
  connector position-loss reconciliation
- Catalog codec encode/decode for PlanNode::Snapshot
- Explain labels in plan and runtime crates
- op_kinds / assert_op_kinds updated in SQL frontend
- Bump version 0.22.0 -> 0.23.0
