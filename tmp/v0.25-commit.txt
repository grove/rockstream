v0.25.0: Lateral, SRF, UDF, and approximate-aggregate surface

Implements all v0.25 roadmap items:

- BloomUnion/v1 law (MergeLawId 0x0006): 256-bit Bloom filter, bitwise-OR
  merge, semilattice, identity = all-zero 32 bytes. LawRegistry now has 6
  built-in laws.

- Plan IR: PlanNode::Lateral {input, func: LateralFunc}, LateralFunc enum
  (Unnest/GenerateSeries/JsonExtractArray), AggregateFunc::ApproxCountDistinct
  (HyperLogLog/v1), AggregateFunc::ApproxMembership (BloomUnion/v1),
  OpKind::Lateral, Expr::ScalarUdf, UdafSpec struct.

- DiffCtx: Lateral -> stateless, ApproxCountDistinct -> HLL_ID,
  ApproxMembership -> BLOOM_UNION_ID.

- Codec: encode/decode for LateralFunc, ScalarUdf, ApproxCountDistinct,
  ApproxMembership.

- Explain: OpKind::Lateral arm in plan and runtime explain.

- Oracle: lateral_srf_oracle module (LateralOracle, ApproxCountDistinctOracle,
  ApproxMembershipOracle, UdafRequirements).

- 13 proof tests: unnest/generate_series/json_extract_array match batch,
  lateral retraction is exact, APPROX_* map to correct law IDs, HLL and
  BloomUnion sketch-union laws are idempotent and commutative, UdafSpec
  documents requirements, ScalarUdf is stateless, Lateral codec roundtrip.

Closes v0.25 roadmap item.
