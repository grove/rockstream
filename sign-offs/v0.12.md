# v0.12 Sign-off

**Signed off**: 2026-05-29

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — all tests pass, 0 failed, 0 ignored
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes — zero warnings
- [x] `cargo fmt --all --check` passes — code is formatted
- [x] `rockstream-catalog` crate created with 42 tests all passing
- [x] `SchemaVersion` struct with versioned column definitions (`ColumnDef`, `DataType`)
- [x] `CatalogStore` — in-memory catalog keyed by `(NamespaceId, name)` for sources and views
- [x] `CatalogStore::register_source` / `register_view` — insert entries, return `AlreadyExists` on duplicate
- [x] `CatalogStore::update_schema` — evolve schema with compatibility check; incompatible → `RS-1002`
- [x] `CatalogStore::store_plan` / `load_plan` — persist and recall view plan bytes
- [x] Compatible-change rules: add nullable column, widen Int32→Int64 / Float32→Float64 — succeed
- [x] Incompatible-change rules: column removal, rename, type narrowing, nullable→non-nullable, add non-nullable without default — return `RS-1002`
- [x] Plan codec: `substrait-ext/rockstream/v1` JSON envelope with `schema_version` field
- [x] Plan codec embeds `(law_id, law_version)` per Aggregate and Union node
- [x] All `PlanNode` variants round-trip: Source, Filter, Project, Map, Aggregate, Union, Join
- [x] All `Expr` variants round-trip: Column, Literal, BinaryOp (all 12 operators)
- [x] All `AggregateFunc` variants encode/decode: Count, Sum, Avg, Min, Max
- [x] `CatalogError::IncompatibleSchemaChange` displays `RS-1002` error code
- [x] `CatalogError::UnknownMergeLaw` displays `RS-5002` error code
- [x] Namespace isolation: same name in different namespaces is independent
- [x] Workspace version bumped to `0.12.0`
- [x] ROADMAP.md v0.12 status updated to `✅ Done`

## Proof Tests (v0.12 Exit Criteria)

- [x] `proof_plan_round_trips_through_catalog_store` — plan survives store→load with identical structure
- [x] `proof_plan_law_annotation_persists_in_wire_bytes` — `law_id`/`law_version` present in JSON bytes
- [x] `proof_all_plan_node_types_round_trip` — all 6 PlanNode kinds round-trip correctly
- [x] `proof_compatible_schema_change_succeeds` — adding nullable column is compatible
- [x] `proof_compatible_type_widening_succeeds` — Int32→Int64 widening is compatible
- [x] `proof_incompatible_column_removal_returns_rs_1002` — removing column → RS-1002
- [x] `proof_incompatible_column_rename_returns_rs_1002` — renaming column → RS-1002
- [x] `proof_incompatible_store_update_returns_rs_1002` — incompatible `update_schema` → RS-1002
- [x] `proof_replay_against_unknown_law_returns_rs_5002` — unknown law in codec.decode → RS-5002
- [x] `proof_replay_against_unknown_law_via_store_returns_rs_5002` — unknown law via store.load_plan → RS-5002
- [x] `proof_known_law_in_plan_decodes_successfully` — WeightAdd/v1 decodes correctly
