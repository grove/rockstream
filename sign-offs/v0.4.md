# v0.4 Sign-off

**Signed off**: 2026-05-29

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.4 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
  - `make e2e` starts a local process, runs the no-op pipeline, and tears down cleanly
  - Audit log contains all required events: `pipeline.created`, `pipeline.started`, `pipeline.stopped`, `server.started`, `server.stopped`
  - Support bundle (`support-bundle-*.json`) is written and contains `audit_events` and `system_info` fields
- [x] ROADMAP.md status updated to `✅ Done`
