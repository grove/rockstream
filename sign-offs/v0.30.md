# Sign-Off: v0.30.0 — Direct Exchange with Planner-Driven Combiners

**Version**: v0.30.0  
**Branch**: `v0.30-exchange`  
**Date**: 2026-05-30

---

## Checklist

- [x] `cargo fmt --check` passes
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [x] `cargo test --workspace` passes (0 failures, 0 skipped)
- [x] Version bumped to `0.30.0` in root `Cargo.toml`
- [x] ROADMAP.md v0.30 row updated to `✅ Done`
- [x] Sign-off file `sign-offs/v0.30.md` present with all items checked
- [x] CI green on the PR branch

---

## Exit Criteria

| Criterion | Status | Evidence |
|---|---|---|
| Loopback path produces zero worker-to-worker network calls for co-located exchanges | ✅ | `loopback_zero_network_calls` — `LoopbackChannel::network_call_count()` returns 0 by construction; `loopback_roundtrip` verifies data flows through in-process channel |
| Combiner proves uncombined-equivalence for all 6 registered laws | ✅ | `weight_add_uncombined_equivalence`, `sum_count_uncombined_equivalence`, `max_register_uncombined_equivalence`, `min_register_uncombined_equivalence`, `hyper_log_log_uncombined_equivalence`, `bloom_union_uncombined_equivalence` |
| Combiner benchmark documents bytes avoided per registered law | ✅ | `weight_add_bytes_avoided_documented`, `sum_count_bytes_avoided_documented`, `max_register_bytes_avoided_documented`, `min_register_bytes_avoided_documented`, `hyper_log_log_bytes_avoided_documented`, `bloom_union_bytes_avoided_documented` |
| No bytes_avoided when batch has no duplicate keys | ✅ | `no_duplicates_no_bytes_avoided` — all-distinct batch produces `bytes_avoided == 0` for all 6 laws |
| Exchange path classifier: same shard + same worker → Elided | ✅ | `classifier_elided_for_same_shard_same_worker` |
| Exchange path classifier: different shards + same worker → Loopback | ✅ | `classifier_loopback_for_same_worker_different_shards` |
| Exchange path classifier: different workers → Direct | ✅ | `classifier_direct_for_different_workers` |
| Arrow IPC encode/decode round-trips a single batch | ✅ | `roundtrip_single_batch`, `roundtrip_empty_batch` |
| Pre-shuffle combiner returns error for unknown law | ✅ | `unknown_law_returns_error` |

---

## Deliverables

### New files

| File | Description |
|---|---|
| `crates/rockstream-types/src/exchange.rs` | `ExchangePath` enum (`Elided`/`Loopback`/`Direct`/`Durable`), `ExchangeAnn` planner annotation |
| `crates/rockstream-runtime/src/exchange/mod.rs` | Exchange module root; re-exports all submodule types |
| `crates/rockstream-runtime/src/exchange/path.rs` | `ExchangeClassifier` — maps source/target worker identities to `ExchangePath` |
| `crates/rockstream-runtime/src/exchange/loopback.rs` | `LoopbackChannel` + `LoopbackReceiver` — bounded in-process mpsc channel with zero network calls |
| `crates/rockstream-runtime/src/exchange/credit.rs` | `CreditTracker` — semaphore-based credit backpressure |
| `crates/rockstream-runtime/src/exchange/combiner.rs` | `PreShuffleCombiner` — law-generic pre-shuffle merge driven by `MergeLawId` |
| `crates/rockstream-runtime/src/exchange/arrow_ipc.rs` | Arrow IPC stream encode/decode helpers (`encode_batch`, `decode_batch`, `encoded_size`) |
| `crates/rockstream-runtime/tests/exchange_combiner.rs` | 17 CI property tests: uncombined-equivalence (1 per law), bytes-avoided benchmark (1 per law), classifier tests, loopback test |

### Modified files

| File | Change |
|---|---|
| `Cargo.toml` | Version bumped 0.29.0 → 0.30.0 |
| `ROADMAP.md` | v0.30 row → `✅ Done` |
| `crates/rockstream-types/src/lib.rs` | Exported `exchange` module |
| `crates/rockstream-types/src/laws/mod.rs` | Re-exported well-known law ID constants (`WEIGHT_ADD_ID`, `SUM_COUNT_ID`, etc.) |
| `crates/rockstream-runtime/src/lib.rs` | Exported `exchange` module |
| `crates/rockstream-runtime/Cargo.toml` | Added `arrow` workspace dependency |

---
