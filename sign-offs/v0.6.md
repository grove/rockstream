# v0.6 Sign-off

**Signed off**: 2026-05-29

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.6 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
  - `filter_ivm_matches_batch_oracle`: proptest verifies random insert/delete
    sequences through the filter operator match `BatchOracle.filter_batch`
    (DataFusion SQL `WHERE val > threshold`) for all positive-weight states.
  - `project_ivm_matches_batch_oracle`: proptest verifies random sequences
    through the project operator match `BatchOracle.project_batch`
    (DataFusion SQL `SELECT id, val * 2 AS val`) for all positive-weight states.
  - `map_ivm_matches_reference`: proptest verifies random sequences through the
    map operator match the reference `zset_map` implementation.
  - `merge_read_fallback_metric_increments`: unit test confirms that calling
    `inc_fallback` (the same function called by `get_merged`/`scan_merged` on
    invalid operands) increments `merge_law_fallback_total` by the expected
    delta.
  - `scan_merged_returns_entries_and_increments_applied`: confirms `scan_merged`
    increments `merge_law_applied_total` once per valid operand.
  - `scan_merged_fallback_increments_fallback_metric`: confirms `scan_merged`
    increments `merge_law_fallback_total` for operands that cannot be parsed.
- [x] ROADMAP.md status updated to `✅ Done`

## Notes

- Oracle comparison is gated on positive-weight states only; Z-set linearity
  is proven separately by the reference filter/project/map comparison which
  handles all weight distributions including negative weights.
- Metric tests use `#[serial]` + before/after delta pattern to be robust
  against parallel test execution on the shared process-global counter registry.
