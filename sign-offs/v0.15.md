# v0.15 Sign-off

**Signed off**: 2026-05-30

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — all tests pass, 0 failed, 0 ignored
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes — zero warnings
- [x] `cargo fmt --all --check` passes — code is formatted
- [x] `DistinctOp` implemented in `rockstream-ops::distinct` with `WeightAdd/v1` zero-crossing state
- [x] `UnionAllOp` implemented in `rockstream-ops::set_ops` — stateless linear passthrough, backed by `WeightAdd/v1`
- [x] `UnionOp` implemented — DISTINCT(UNION ALL), combined-weight zero-crossing, backed by `WeightAdd/v1`
- [x] `IntersectAllOp` implemented — per-side weight tracking, min-clamp output (`not_merge_safe_reason=clamp_not_a_law`)
- [x] `IntersectOp` implemented — set intersection via zero-crossing on both-sides-positive predicate
- [x] `ExceptAllOp` implemented — max(0, left_w − right_w) output clamp (`not_merge_safe_reason=clamp_not_a_law`)
- [x] `ExceptOp` implemented — set difference: present iff left_w > 0 AND right_w == 0
- [x] `CLAMP_NOT_A_LAW` constant defined and returned by all clamped operators via `not_merge_safe_reason()`
- [x] Clamped operators (`IntersectAll`, `Intersect`, `ExceptAll`, `Except`) return `None` from `merge_law()` — compaction disabled
- [x] Law-backed operators (`Distinct`, `UnionAll`, `Union`) return `Some(WEIGHT_ADD_ID)` from `merge_law()`
- [x] `SetOpOracle` implemented in `rockstream-oracle::set_op_oracle` — batch reference for all 7 operations
- [x] `random_distinct_matches_oracle` — proptest: IVM DISTINCT == oracle
- [x] `random_union_all_matches_oracle` — proptest: IVM UNION ALL == oracle
- [x] `random_union_matches_oracle` — proptest: IVM UNION == oracle
- [x] `random_intersect_all_matches_oracle` — proptest: IVM INTERSECT ALL == oracle
- [x] `random_intersect_matches_oracle` — proptest: IVM INTERSECT == oracle
- [x] `random_except_all_matches_oracle` — proptest: IVM EXCEPT ALL == oracle
- [x] `random_except_matches_oracle` — proptest: IVM EXCEPT == oracle
- [x] `clamp_operators_report_no_merge_law` — all 4 clamped operators return `None`
- [x] `law_backed_operators_report_weight_add` — all 3 law-safe operators return `WEIGHT_ADD_ID`
- [x] `law_equiv_distinct_combined_vs_uncombined` — proptest: combined delta == separate deltas
- [x] `law_equiv_union_combined_vs_uncombined` — proptest: combined delta == separate deltas
- [x] `except_supplier_exclusion_scenario` — EXCEPT anti-pattern: supplier exclusion and re-inclusion
- [x] `intersect_full_retraction_scenario` — INTERSECT full-retraction and re-insertion
- [x] `union_all_weight_accumulation_scenario` — UNION ALL weight accumulation correctness
- [x] `compaction_disabled_for_clamp_operators` — all clamped ops return `None` from `merge_law()`
- [x] Workspace version bumped to `0.15.0`
- [x] ROADMAP.md v0.15 status updated to `✅ Done`

## Proof Tests (v0.15 Exit Criteria)

| Test | Result |
|---|---|
| `random_distinct_matches_oracle` (proptest) | ✅ |
| `random_union_all_matches_oracle` (proptest) | ✅ |
| `random_union_matches_oracle` (proptest) | ✅ |
| `random_intersect_all_matches_oracle` (proptest) | ✅ |
| `random_intersect_matches_oracle` (proptest) | ✅ |
| `random_except_all_matches_oracle` (proptest) | ✅ |
| `random_except_matches_oracle` (proptest) | ✅ |
| `clamp_operators_report_no_merge_law` | ✅ |
| `law_backed_operators_report_weight_add` | ✅ |
| `law_equiv_distinct_combined_vs_uncombined` (proptest) | ✅ |
| `law_equiv_union_combined_vs_uncombined` (proptest) | ✅ |
| `except_supplier_exclusion_scenario` | ✅ |
| `intersect_full_retraction_scenario` | ✅ |
| `union_all_weight_accumulation_scenario` | ✅ |
| `compaction_disabled_for_clamp_operators` | ✅ |
