# v0.3 Sign-off

**Signed off**: 2026-05-29

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.3 have been verified.

- [x] `cargo test --workspace` passes
- [x] All Proof criteria verified against running code or CI output
  - Storage API validation suite passes: only supported SlateDB features used (single-writer fencing, `WriteBatch`, `DbReader`, checkpoints, `MergeOperator`, TTL, WAL reader)
  - No code path depends on range deletion
  - Catalog key encoders include `namespace_id` dimension
  - **SlateDB determinism gate**: `determinism_two_runs_identical_state` and `determinism_interleaved_operations` both pass — two runs at the same seed against `InMemory` object store produce bit-identical key–value state
- [x] ROADMAP.md status updated to `✅ Done`
