# v0.11 Sign-off

**Signed off**: 2026-05-29

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — all tests pass, 0 failed, 0 ignored
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes — zero warnings
- [x] `cargo fmt --all --check` passes — code is formatted
- [x] `SqlFrontend::parse_statement` parses SELECT, CREATE VIEW, and rejects invalid SQL
- [x] `SqlFrontend::lower` implemented: lowers DataFusion `LogicalPlan` to `PlanNode`
- [x] `TableScan` → `PlanNode::Source` with correct table name
- [x] `EmptyRelation` → `PlanNode::Source { name: "<empty>" }`
- [x] `Projection` → `PlanNode::Project` with column expressions
- [x] `Filter` → `PlanNode::Filter` with lowered predicate expression
- [x] `Aggregate` → `PlanNode::Aggregate` with law-mapped aggregate functions
- [x] `Union` → `PlanNode::Union` (folded pairwise from n inputs)
- [x] `SubqueryAlias` transparent — inner plan lowered correctly
- [x] Aggregate function mapping: SUM/COUNT/AVG → `AggregateFunc::{Sum,Count,Avg}` → `WeightAdd/v1`
- [x] Aggregate function mapping: MIN → `AggregateFunc::Min` → `MinRegister/v1` + `ExtremumRequiresRmw`
- [x] Aggregate function mapping: MAX → `AggregateFunc::Max` → `MaxRegister/v1` + `ExtremumRequiresRmw`
- [x] Scalar expression lowering: Column, Literal, BinaryExpr (all Phase 1 operators), Cast, TryCast, Case, Alias
- [x] Binary operator mapping: Eq, Ne, Lt, Le, Gt, Ge, Add, Sub, Mul, Div, And, Or
- [x] Proof: `sql_plan_matches_hand_built_aggregate_sum_phase1_proof` — SQL-lowered and hand-built plans produce identical operator kinds, law annotations, and not-merge-safe reasons
- [x] Proof: `sql_plan_matches_hand_built_filter_project_phase1_proof` — filter+project structure matches
- [x] Proof: `all_aggregate_nodes_have_law_or_reason` — every aggregate node has a merge_law OR not_merge_safe_reason for all 5 Phase 1 aggregate functions (SUM, COUNT, AVG, MIN, MAX)
- [x] Proof: `explain_output_covers_all_aggregate_laws` — EXPLAIN output shows correct law name and not_merge_safe_reason for every Phase 1 aggregate
- [x] `lower_table_scan_produces_source` — TableScan lowering verified
- [x] `lower_filter_op_structure_matches_hand_built` — Filter lowering verified
- [x] `lower_projection_op_structure_matches_hand_built` — Projection lowering verified
- [x] `lower_aggregate_sum/count/avg/max/min_gets_*` — individual aggregate law tests pass
- [x] Criterion throughput bench file compiles and builds correctly (tested via `cargo clippy --all-targets`)
- [x] ROADMAP.md v0.11 status updated to `✅ Done`
- [x] Version bumped to `0.11.0` across workspace
