# v0.14 Sign-off

**Signed off**: 2026-06-01

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — all tests pass, 0 failed, 0 ignored
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes — zero warnings
- [x] `cargo fmt --all --check` passes — code is formatted
- [x] `OuterJoinOp` implemented in `rockstream-ops::outer_join` with `JoinType` enum
- [x] `JoinType` variants: `LeftOuter`, `RightOuter`, `FullOuter`, `LeftSemi`, `LeftAnti`, `RightSemi`, `RightAnti`
- [x] Unmatched-row state: `right_weight_by_jk` and `left_weight_by_jk` track per-join-key total weights
- [x] Zero-crossing logic: null-padded rows emitted when total weight crosses zero (0 → positive); retracted when positive → 0
- [x] `process_left_delta` — output inner products + FullOuter null-right tracking
- [x] `process_right_delta` — output inner products + LeftOuter/FullOuter null-left tracking
- [x] `process_epoch` — combined left+right delta
- [x] `compact()` — removes zero-weight entries from all arrangements
- [x] `metadata()` → `OuterJoinMeta` — join key counts and row counts for `EXPLAIN INCREMENTAL`
- [x] `NullCombineFn` type alias for null-padded row construction
- [x] `OuterJoinOracle` implemented in `rockstream-oracle::join_oracle` — batch reference for correctness
- [x] `OuterJoinOracle::compute_left_outer_join()` — algebraic: inner products + null-padded unmatched left rows
- [x] `OuterJoinOracle::compute_right_outer_join()` — algebraic: inner products + null-padded unmatched right rows
- [x] `OuterJoinOracle::compute_full_outer_join()` — inner products + null-padded unmatched from both sides
- [x] `OuterJoinOracle::compute_left_semi_join()` — left rows present in any matching right
- [x] `OuterJoinOracle::compute_left_anti_join()` — left rows with no matching right
- [x] `OuterJoinOracle::compute_right_semi_join()` — right rows present in any matching left
- [x] `OuterJoinOracle::compute_right_anti_join()` — right rows with no matching left
- [x] `random_left_outer_join_matches_oracle` — proptest: IVM LEFT OUTER == oracle
- [x] `random_right_outer_join_matches_oracle` — proptest: IVM RIGHT OUTER == oracle
- [x] `random_full_outer_join_matches_oracle` — proptest: IVM FULL OUTER == oracle
- [x] `random_left_semi_join_matches_oracle` — proptest: IVM LEFT SEMI == oracle
- [x] `random_left_anti_join_matches_oracle` — proptest: IVM LEFT ANTI == oracle
- [x] `q11_style_left_outer_join_null_heavy` — Q11-style: unmatched suppliers emit null-padded rows
- [x] `q11_style_full_outer_join_edge_case` — Q11 FULL OUTER: both sides have unmatched rows
- [x] `q21_style_anti_join_sole_suppliers` — Q21-style: LEFT ANTI detects sole suppliers
- [x] `q21_style_anti_join_competitor_removed` — Q21: retraction of competitor re-emits sole supplier
- [x] `full_outer_join_all_matched_no_nulls` — FULL OUTER with all matched: no null rows
- [x] `null_heavy_left_outer_join_matches_oracle` — proptest, restricted keys 1..=2 for high NULL rate
- [x] `left_outer_incremental_sorted_matches_oracle` — proptest: sorted epoch sequence matches oracle
- [x] Workspace version bumped to `0.14.0`
- [x] ROADMAP.md v0.14 status updated to `✅ Done`

## Proof Tests (v0.14 Exit Criteria)

| Test | Result |
|---|---|
| `random_left_outer_join_matches_oracle` (proptest) | ✅ |
| `random_right_outer_join_matches_oracle` (proptest) | ✅ |
| `random_full_outer_join_matches_oracle` (proptest) | ✅ |
| `random_left_semi_join_matches_oracle` (proptest) | ✅ |
| `random_left_anti_join_matches_oracle` (proptest) | ✅ |
| `q11_style_left_outer_join_null_heavy` | ✅ |
| `q11_style_full_outer_join_edge_case` | ✅ |
| `q21_style_anti_join_sole_suppliers` | ✅ |
| `q21_style_anti_join_competitor_removed` | ✅ |
| `full_outer_join_all_matched_no_nulls` | ✅ |
| `null_heavy_left_outer_join_matches_oracle` (proptest, high NULL rate) | ✅ |
| `left_outer_incremental_sorted_matches_oracle` (proptest) | ✅ |
