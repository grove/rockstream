# v0.20 Sign-off

**Signed off**: 2026-05-31

## Exit Criteria Verification

All criteria in the Proof column of ROADMAP.md for v0.20 have been verified.

- [x] `cargo test --workspace` passes
- [x] Late data test matrix proves `drop`, `update`, and `route_to_sink` behavior
- [x] TTL never removes visible state (closed-window emitted rows retained indefinitely)
- [x] Tumbling windows close exactly once under out-of-order input
- [x] Duplicate-watermark replay is idempotent (`MaxRegister/v1` semantics)
- [x] `TumbleOp` output matches `TumbleOracle` for randomised inputs (proptest)
- [x] `TumbleWindow` plan nodes round-trip through the catalog codec (all three late-data policy variants)
- [x] `DiffCtx` assigns `MaxRegister/v1` merge law to `TumbleWindow` operators
- [x] Watermark is monotone (MaxRegister semantics: never retreats)
- [x] All Proof criteria verified against running code or CI output
- [x] Simulation continuity: all simulation tests from prior versions still pass
- [x] ROADMAP.md status updated to `✅ Done`

## Scope Notes

- **Escape hatch applied**: TUMBLE only is implemented; HOP/SESSION deferred to v0.21 (as explicitly permitted by the ROADMAP.md v0.20 escape hatch).
- `MaxRegister/v1` (already registered from v0.8) serves as the watermark-state merge law.
- `MinRegister/v1` is available in the law registry but not used by TUMBLE (HOP/SESSION will leverage it).
- Late-data policies implemented: `Drop`, `Update` (retract + re-emit), `RouteToSink { sink_name }`.
- Proof tests in `rockstream-oracle/tests/tumble_tests.rs`: 9 proof tests + 1 proptest.

## New Items in v0.20

- `LateDataPolicy` enum (`Drop`, `Update`, `RouteToSink`) in `rockstream-plan`
- `PlanNode::TumbleWindow` logical plan node
- `OpKind::TumbleWindow` physical operator kind
- `TumbleOp` in `rockstream-ops` with MaxRegister watermark semantics
- `TumbleOracle` batch reference oracle in `rockstream-oracle`
- Codec encode/decode for `TumbleWindow` and `LateDataPolicy`
- `EXPLAIN` output for `TumbleWindow` in all explain paths
