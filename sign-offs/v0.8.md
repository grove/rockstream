# v0.8 Sign-off

**Signed off**: 2026-05-29

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — all tests pass, 0 failed, 0 ignored
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes — zero warnings
- [x] `cargo fmt --all --check` passes — code is formatted
- [x] `cargo deny check` passes — licenses and advisories clean
- [x] `MaxRegister/v1` law (`max_register.rs`) implemented: semilattice merge `max(a,b)`, identity `i64::MIN`, ID 0x0003
- [x] `MinRegister/v1` law (`min_register.rs`) implemented: semilattice merge `min(a,b)`, identity `i64::MAX`, ID 0x0004
- [x] Both laws registered in `LawRegistry::with_builtins()` — now 4 builtins total
- [x] Storage merge operator extended with `MergeTag::MaxRegister` (0x03) and `MergeTag::MinRegister` (0x04)
- [x] `MinMaxOp` implemented with indexed multiset state (`BTreeMap<i64, i64>` per group)
- [x] Delete path via prefix scan: BTreeMap ordered iteration finds new extremum in O(log n)
- [x] `MinMaxOp::merge_law()` reports `MAX_REGISTER_ID` / `MIN_REGISTER_ID` as cached-slot law (EXPLAIN INCREMENTAL)
- [x] `DiffCtx::law_for_aggregate()` updated: MAX→`MaxRegister/v1`, MIN→`MinRegister/v1` with `ExtremumRequiresRmw`
- [x] `diff_min_aggregate_not_merge_safe` test updated to assert `merge_law == Some(MIN_REGISTER_ID)`
- [x] New `diff_max_aggregate_uses_max_register` test added
- [x] `MinMaxOracle` implemented using DataFusion SQL (`MIN(val)` / `MAX(val) GROUP BY group_id`)
- [x] `zset_min_max` reference implementation for arbitrary Z-sets (with retractions)
- [x] >=100k randomized MAX group churn scenarios match reference (`max_100k_scenarios_match_reference`)
- [x] >=100k randomized MIN group churn scenarios match reference (`min_100k_scenarios_match_reference`)
- [x] 100k insert-only MAX scenarios match DataFusion oracle (`max_100k_matches_datafusion_oracle`)
- [x] Stale operand proof: `stale_max_operand_cannot_reduce_stored_max` — max(10, 3) = 10, stale 3 cannot corrupt
- [x] Stale operand proof: `stale_min_operand_cannot_increase_stored_min` — min(3, 8) = 3, stale 8 cannot corrupt
- [x] Delete path proof: `delete_path_prefix_scan_finds_new_max` — after deleting max=10, BTreeMap rescan finds 7
- [x] EXPLAIN proof: `diff_ctx_explain_incremental_uses_register_laws` in integration tests
- [x] `MaxRegister/v1` and `MinRegister/v1` pass the law property harness (associativity, commutativity, idempotence, identity, serde round-trip)
- [x] Arrangement header round-trips via `ShardDb` for both new laws
- [x] `max_register_merge_via_shard_db` and `min_register_merge_via_shard_db` storage correctness proofs
- [x] ROADMAP.md status updated to `✅ Done`
