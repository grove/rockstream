# v0.9 Sign-off

**Signed off**: 2026-05-29

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — all tests pass, 0 failed, 0 ignored
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes — zero warnings
- [x] `cargo fmt --all --check` passes — code is formatted
- [x] `cargo deny check` passes — licenses and advisories clean
- [x] `EpochCoordinator` implements shard-level group commit (single atomic `WriteBatch` per epoch)
- [x] Persisted frontier written atomically with state data in every `commit_epoch` `WriteBatch`
- [x] `read_frontier()` recovers last committed epoch number for crash/restart replay
- [x] `WalListingCache` hot path issues zero object-store `list()` calls after initial `populate()`
- [x] `SchedulerConfig::max_rows_per_quantum` (default 65536) bounds operator processing per quantum
- [x] Quantum-bounded `spawn_operator_task_with_config` yields via `tokio::task::yield_now()` between chunks
- [x] `YieldCounter::yield_ratio()` (`scheduler_yield_ratio`) metric correctly reports cooperative yield fraction
- [x] Kill-inject proof: `kill_inject_restart_frontier_survives` — frontier survives simulated crash+restart, state bit-identical
- [x] Kill-inject proof: `idempotent_replay_produces_bit_identical_state` — replaying committed epoch produces same state
- [x] Kill-inject proof: `frontier_reads_back_exact_epoch_after_restart` — frontier is exact after 5 committed epochs and restart
- [x] Kill-inject proof: `multi_operator_coalesced_batch_survives_restart` — coalesced multi-operator batch durable across restart
- [x] WAL proof: `wal_listing_cache_hot_path_issues_no_list_calls` — `list_call_count == 1` after 1000 hot-path cache reads
- [x] WAL proof: `wal_listing_cache_rotation_counts_as_one_list` — invalidate+repopulate counts as exactly one additional list call
- [x] WAL proof: `wal_listing_cache_starts_empty` — fresh cache has no entries and zero list calls
- [x] Heartbeat proof: `scheduler_yield_ratio_nonzero_for_oversized_epoch` — yield ratio > 0 when epoch exceeds quantum
- [x] Heartbeat proof: `scheduler_no_yield_for_small_epoch` — no yield when epoch fits in one quantum
- [x] Heartbeat proof: `large_epoch_does_not_starve_heartbeat_task` — concurrent heartbeat task ticks during operator quantum processing
- [x] Heartbeat proof: `yield_ratio_across_multiple_epochs` — yield ratio accumulates correctly across multiple epochs
- [x] All 11 integration tests in `crates/rockstream-runtime/tests/epoch_commit_tests.rs` pass
- [x] ROADMAP.md status updated to `✅ Done`
