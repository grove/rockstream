# v0.13 Sign-off

**Signed off**: 2026-05-31

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — all tests pass, 0 failed, 0 ignored
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes — zero warnings
- [x] `cargo fmt --all --check` passes — code is formatted
- [x] `HashJoinOp` implemented in `rockstream-ops::join` with dual Z-set arrangements
- [x] Dual arrangements: `left_arr` and `right_arr`, each `HashMap<join_key, HashMap<(row_key, row_value), weight>>`
- [x] Stable row identity: row identity is `row_key` bytes; value bytes carry the join-key column
- [x] Pre-change snapshot semantics: output is computed against snapshot BEFORE arrangement update
- [x] `process_left_delta` and `process_right_delta` — explicit two-phase: output then update
- [x] `process_epoch` — combined left+right delta (left processed first, right processed with updated left)
- [x] `compact()` — removes zero-weight entries from both arrangements at epoch boundaries
- [x] `metadata()` → `JoinMeta` — join key counts and row counts for `EXPLAIN INCREMENTAL`
- [x] `JoinOracle` implemented in `rockstream-oracle::join_oracle` — batch reference for correctness
- [x] `JoinOracle::compute_join()` — bilinear join formula over accumulated left/right state
- [x] `JoinRowSchema` and `JoinRowWithValSchema` — reusable schema helpers for test rows
- [x] `random_inner_join_matches_oracle` — proptest: IVM accumulated output == oracle batch join
- [x] `three_way_join_matches_oracle` — proptest: chained A⊗B⊗C IVM matches chained oracle
- [x] `q1_style_single_table_aggregate_baseline` — baseline: filter + aggregate, no join
- [x] `q3_style_customer_orders_join` — 2-table inner join with cardinality check (3 matched rows)
- [x] `q3_style_right_before_left` — right side loaded first, left arrives late
- [x] `q5_style_supplier_orders_join_with_sum` — supplier ⊗ orders join with unmatched row
- [x] `q5_style_late_arriving_left_rows` — late-arriving left side
- [x] `q6_style_filter_aggregate` — single-table filter + aggregate
- [x] `single_match_produces_one_row` — deterministic single-pair join
- [x] `retraction_removes_joined_row` — row retraction removes its join product
- [x] `many_to_many_join_cardinality` — 3×3 many-to-many produces 9 rows
- [x] Workspace version bumped to `0.13.0`
- [x] ROADMAP.md v0.13 status updated to `✅ Done`

## Proof Tests (v0.13 Exit Criteria)

| Test | Result |
|---|---|
| `random_inner_join_matches_oracle` (proptest) | ✅ |
| `three_way_join_matches_oracle` (proptest, chained joins) | ✅ |
| `q1_style_single_table_aggregate_baseline` | ✅ |
| `q3_style_customer_orders_join` | ✅ |
| `q3_style_right_before_left` | ✅ |
| `q5_style_supplier_orders_join_with_sum` | ✅ |
| `q5_style_late_arriving_left_rows` | ✅ |
| `q6_style_filter_aggregate` | ✅ |
| `single_match_produces_one_row` | ✅ |
| `retraction_removes_joined_row` | ✅ |
| `many_to_many_join_cardinality` | ✅ |
