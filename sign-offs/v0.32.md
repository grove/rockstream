# Sign-Off: v0.32.0 — Frontier Protocol with Law-Aware Partial Progress

**Version**: v0.32.0  
**Branch**: `v0.32-frontier-protocol`  
**Date**: 2026-05-30

---

## Checklist

- [x] `cargo fmt --check` passes
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [x] `cargo test --workspace` passes (0 failures, 0 skipped)
- [x] Version bumped to `0.32.0` in root `Cargo.toml`
- [x] ROADMAP.md v0.32 row updated to `✅ Done`
- [x] Sign-off file `sign-offs/v0.32.md` present with all items checked
- [x] CI green on the PR branch

---

## Exit Criteria

| Criterion | Status | Evidence |
|---|---|---|
| Multi-input join with uneven sources produces no premature output | ✅ | `multi_input_join_no_premature_output` — `JoinFrontierGuard` blocks epoch E output until both input frontiers have advanced past E |
| Aggregator stress test covers thousands of shards × hundreds of operators without direct per-shard subscriptions | ✅ | `aggregator_stress_many_shards_operators` — 1 000 shards feed one `WorkerFrontierAggregator`; 100 operator consumers read the single worker summary (zero per-shard channels) |
| A monotone-recursion view emits partial progress with a frontier-tagged completeness token | ✅ | `monotone_view_emits_complete_through_token` — all 4 semilattice laws (MaxRegister, MinRegister, HLL, BloomUnion) produce `CompleteThroughToken`; 2 abelian-group laws (WeightAdd, SumCount) return `None` |

---

## Deliverables

### New files

| File | Description |
|---|---|
| `crates/rockstream-runtime/src/frontier.rs` | `FrontierRole`, `ShardFrontierReporter`, `WorkerFrontierAggregator`, `ClusterFrontierPublisher`, `LawRegistryLike`, `is_monotone_law`, `try_complete_through`, `ShuffleGc`, `ShuffleOutboxRecord` |
| `crates/rockstream-runtime/tests/frontier_protocol.rs` | 10 CI proof tests covering all ROADMAP exit criteria plus structural invariants |

### Modified files

| File | Change |
|---|---|
| `Cargo.toml` | Version bumped 0.31.0 → 0.32.0 |
| `ROADMAP.md` | v0.32 row → `✅ Done` |
| `crates/rockstream-runtime/src/lib.rs` | Added `pub mod frontier;` |
| `crates/rockstream-types/src/frontier.rs` | Extended with `ShardFrontierReport`, `WorkerFrontierSummary`, `ClusterFrontier`, `CompleteThroughToken` and their tests |

---

## Additional tests (structural proofs)

| Test | What it proves |
|---|---|
| `shard_reporter_advance_sends_reports` | Reporter sends to aggregator; aggregator takes per-shard max (monotone-safe) |
| `cluster_frontier_none_until_all_workers_report` | Cluster frontier is `None` while any worker has not reported |
| `shuffle_gc_collects_exactly_below_frontier` | GC deletes entries with epoch < frontier; entries at exactly the frontier are kept |
| `frontier_role_parse_and_format` | `--role=frontier` / `--role=compute` parse and format correctly |
| `poll_returns_none_when_nothing_changed` | `WorkerFrontierAggregator::poll` is idempotent |
| `cluster_frontier_is_exact_meet` | Cluster frontier = meet (minimum) of all worker summaries |
| `single_worker_cluster_frontier_equals_worker_min_epoch` | Single-worker cluster frontier equals that worker's frontier |
