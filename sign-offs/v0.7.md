# v0.7 Sign-off

**Signed off**: 2026-05-29

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — all tests pass, 0 failed, 0 ignored
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes — zero warnings
- [x] `cargo fmt --all --check` passes — code is formatted
- [x] `cargo deny check` passes — licenses and advisories clean
- [x] `SumCount/v1` law (`sum_count.rs`) implemented with full unit tests
- [x] `SumCountV1` registered in `LawRegistry::with_builtins()` (ID 0x0002)
- [x] `AggregateMergeOp` implemented on top of `LawBundle` with last-emitted cache
- [x] Arrangement headers carry `(law_id, law_version)` via `put_with_arrangement_header`
- [x] `get_arrangement_header` reads back `(law_id, law_version)` on mount
- [x] >=100k randomized aggregate scenarios match the reference oracle (`aggregate_100k_scenarios_match_reference`)
- [x] `0xAG` arrangement headers verified via `arrangement_header_round_trips_on_mount`
- [x] Benchmark captures baseline throughput and per-law RMW-avoidance ratio (`bench_aggregate_throughput_and_rmw_avoidance`) — 100% RMW-avoidance for SumCount/v1 (abelian group)
- [x] `AggregateOracle` implemented using DataFusion SQL for oracle comparison
- [x] Storage tests added for arrangement header round-trip and missing-key paths
- [x] ROADMAP.md status updated to `✅ Done`
