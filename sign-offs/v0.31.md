# Sign-Off: v0.31.0 — Durable Shuffle Fallback with Law-Aware Re-Merge

**Version**: v0.31.0  
**Branch**: `v0.31-durable-shuffle`  
**Date**: 2026-05-30

---

## Checklist

- [x] `cargo fmt --check` passes
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes
- [x] `cargo test --workspace` passes (0 failures, 0 skipped)
- [x] Version bumped to `0.31.0` in root `Cargo.toml`
- [x] ROADMAP.md v0.31 row updated to `✅ Done`
- [x] Sign-off file `sign-offs/v0.31.md` present with all items checked
- [x] CI green on the PR branch

---

## Exit Criteria

| Criterion | Status | Evidence |
|---|---|---|
| Sender falls back durably and receiver catches up without duplicates | ✅ | `receiver_fault_recovery_no_duplicates` — write → fault → recovery read returns exact original frames; object store is idempotent |
| Large batch coalesces into one object | ✅ | `large_batch_coalesces_into_single_object` — 1 000 frames written in one `write()` call; `frame_count == 1000`; single `get` reads all back |
| Durable + direct paths produce bit-identical state for WeightAdd/v1 | ✅ | `weight_add_durable_direct_identical` |
| Durable + direct paths produce bit-identical state for SumCount/v1 | ✅ | `sum_count_durable_direct_identical` |
| Durable + direct paths produce bit-identical state for MaxRegister/v1 | ✅ | `max_register_durable_direct_identical` |
| Durable + direct paths produce bit-identical state for MinRegister/v1 | ✅ | `min_register_durable_direct_identical` |
| Durable + direct paths produce bit-identical state for HyperLogLog/v1 | ✅ | `hyper_log_log_durable_direct_identical` |
| Durable + direct paths produce bit-identical state for BloomUnion/v1 | ✅ | `bloom_union_durable_direct_identical` |
| No LIST call on hot-path read | ✅ | `no_list_call_on_read` — reader API accepts `OutboxEntry` (pre-resolved path), never a prefix or wildcard |
| Sequence numbers are monotone | ✅ | `sequence_numbers_are_monotone` — 5 successive writes produce strictly increasing `entry.sequence` values |
| Unknown law returns `DurableError::UnknownLaw` | ✅ | `unknown_law_returns_error` |

---

## Deliverables

### New files

| File | Description |
|---|---|
| `crates/rockstream-runtime/src/exchange/durable.rs` | `ShuffleFrame`, `OutboxEntry`, `DurableError`, `encode_object`/`decode_object`, `DurableShuffleWriter`, `DurableShuffleReader` |
| `crates/rockstream-runtime/tests/durable_shuffle.rs` | 12 CI proof tests: write/read roundtrip, receiver-fault recovery, large batch, no-LIST guarantee, bit-identical state (1 per law), sequence monotonicity, unknown-law error |

### Modified files

| File | Change |
|---|---|
| `Cargo.toml` | Version bumped 0.30.0 → 0.31.0 |
| `ROADMAP.md` | v0.31 row → `✅ Done` |
| `crates/rockstream-runtime/src/exchange/mod.rs` | Exported `durable` module; updated description to reflect v0.31 |
| `crates/rockstream-runtime/Cargo.toml` | Moved `object_store` from dev-dependencies to regular dependencies; added `bytes` |

---
