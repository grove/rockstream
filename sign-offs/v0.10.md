# v0.10 Sign-off

**Signed off**: 2026-05-29

## Exit Criteria Verification

- [x] `cargo test --workspace` passes — all tests pass, 0 failed, 0 ignored
- [x] `cargo clippy --workspace --all-targets -- -D warnings` passes — zero warnings
- [x] `cargo fmt --all --check` passes — code is formatted
- [x] `cargo deny check` passes — licenses and advisories clean
- [x] `GenerateRowsSource` built-in synthetic data source produces rows immediately, zero external dependencies
- [x] `GenerateRowsConfig` supports configurable `rows_per_epoch`, `seed`, `product_id_range`, `quantity_max`
- [x] `GenerateRowsSource::poll_batch` always returns `Some(batch)` — never blocks waiting for upstream
- [x] Row generation is reproducible: same seed produces same sequence across restart
- [x] `rockstream explain <view>` command wired into the binary (Explain subcommand)
- [x] `explain_plan` uses `LawRegistry::with_builtins()` to annotate every operator with its merge law
- [x] `explain_plan` emits `merge_law = "WeightAdd/v1"` for SUM, COUNT, AVG, DISTINCT aggregates
- [x] `explain_plan` emits `not_merge_safe_reason` for MIN and MAX (non-invertible operators)
- [x] `render_explain` produces human-readable table with op_id, kind, merge_law, not_merge_safe_reason columns
- [x] Embedded benchmark `run_embedded_bench` reports p50 and p95 epoch commit latency in milliseconds
- [x] Embedded benchmark `shuffle_calls = 0` — no gRPC shuffle in embedded profile
- [x] `SupportBundle` extended with `plan_stats: Vec<PlanStatRow>` and `shard_stats: ShardStats`
- [x] `shard_stats` counts existing `.json` bundle files and `.jsonl` audit log files
- [x] Developer alpha loop proof: feed 3 epochs, simulate crash (drop db handle), restart, frontier survives
- [x] Proof: `GENERATE ROWS` source produces 100 rows per epoch immediately at first call
- [x] Proof: embedded benchmark `embedded_bench_reports_p50_p95` — p50/p95 present and p95 >= p50
- [x] Proof: embedded benchmark `embedded_bench_zero_shuffle_calls` — shuffle_calls == 0
- [x] Proof: `explain_shows_weight_add_for_sum/count/avg/distinct` — merge law annotated correctly
- [x] Proof: `explain_shows_not_merge_safe_for_max/min` — reason string present for non-invertible ops
- [x] Integration tests in `rockstream-runtime/tests/dev_alpha_tests.rs` cover all 4 proof criteria
