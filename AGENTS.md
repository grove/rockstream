# Agent Configuration

## Rules

- Never use HEREDOC.
- After every git commit make sure that the commit messages isn't garbled.
- After creating or updating a pull request title or body ake sure that they aren't garbled.